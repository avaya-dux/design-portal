---
import { Code } from "astro/components";

export interface Props {
  code: string;
  lang?: "jsx" | "js" | "html" | "md" | "bash";
}

const { code, lang = "jsx" } = Astro.props as Props;
---

<Code code={code} lang={lang} />
