---
export interface Props {
  href: string;
  inline?: boolean;
}
import { Link } from "@avaya/neo-react";

const { href, inline = true } = Astro.props as Props;
---

<Link inline={inline} href={href} target="_blank" rel="noopener noreferrer">
  <slot />
</Link>
