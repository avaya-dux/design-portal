---
import {
	CollapsibleColumns,
	Description,
	ImageWithCaption,
	PageleteSection,
	SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/sound-use-1.svg";
import image2 from "../imgs/sound-use-2.svg";
---

<PageleteSection>
  <SectionHeader id={topics.whenToUse.id}>
    {topics.whenToUse.title}
  </SectionHeader>

  <Description>
    As with our foundational components, sound should be used to convey
    information in meaningful context. There are some situations where sounds
    are a useful addition.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a telephone icon with sound waves emanating from it."
      captionTitle="Telephony Feedback"
    >
      Traditional telephony sounds, such as incoming and outgoing ringtones,
      dial tones, and conferencing tones when entering and exiting a space, all
      help to convey the current state within the environment.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing notification and alert UI elements."
      captionTitle="Notifications and Alerts"
    >
      In-product notifications and alerts can highlight these elements.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
