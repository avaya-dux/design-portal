---
import { Link } from "@avaya/neo-react";
import {
  CollapsibleColumns,
  Description,
  ImageWithCaption,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/data-vis-common-guidelines-1.svg";
import image2 from "../imgs/data-vis-common-guidelines-2.svg";
import image3 from "../imgs/data-vis-common-guidelines-3.svg";
import image4 from "../imgs/data-vis-common-guidelines-4.svg";
import image5 from "../imgs/data-vis-common-guidelines-5.svg";
---

<PageleteSection>
  <SectionHeader id={topics.guidelines.id}>
    {topics.guidelines.title}
  </SectionHeader>

  <Description>
    These guidelines apply to all data visualization presentation types.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a 3D chart crossed out."
      captionTitle="Avoid 3D Charts"
    >
      Using 3D charts reduces readability and provides no additional value.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing a palette with different colors."
      captionTitle="Use Neo's Color Palette"
    >
      Use the color palette provided by Neo to ensure data has sufficient
      contrast and is accessible for all users. See
      <Link inline href="/guidelines/color">Colors to learn more.</Link>
    </ImageWithCaption>
  </CollapsibleColumns>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image3.src}
      imgAlt="Image showing a tooltip over a line chart data point."
      captionTitle="Use Tooltips"
    >
      Tooltips should be used to provide supplemental information on a chart
      (e.g. date or data value). They should not be the only way to identify the
      data.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image4.src}
      imgAlt="Image showing legends for each data series in a chart."
      captionTitle="Include Legends"
    >
      Include legends when more than one data series is shown.
    </ImageWithCaption>
  </CollapsibleColumns>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image5.src}
      imgAlt="Image showing a visual gap in a line chart to depict missing data."
      captionTitle="Indicate When Data Is Missing"
    >
      When a set of data includes null or missing data, the chart should reflect
      this with a space rather than using '0' or connecting points.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
