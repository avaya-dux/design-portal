---
import {
	CollapsibleColumns,
	Description,
	ImageWithCaption,
	PageleteSection,
	SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/data-vis-bar-charts-1.svg";
import image2 from "../imgs/data-vis-bar-charts-2.svg";
import image3 from "../imgs/data-vis-bar-charts-3.svg";
import image4 from "../imgs/data-vis-bar-charts-4.svg";
---

<PageleteSection>
  <SectionHeader id={topics.barcharts.id}>
    {topics.barcharts.title}
  </SectionHeader>

  <Description>
    Bar charts are used to compare different values that have the same level of
    importance.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a vertically oriented bar chart."
      captionTitle="Standard Bar Chart"
    >
      Displays one category per bar, and can be oriented vertically or
      horizontally.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing a stacked bar chart with clear gaps between sections."
      captionTitle="Stacked Bar Chart"
    >
      Displays multiple subcategories within each data set. Sections should be
      divided with whitespace to provide clear contrast between elements.
    </ImageWithCaption>
  </CollapsibleColumns>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image3.src}
      imgAlt="Image showing a bar chart where the x-axis values are sorted."
      captionTitle="Order The Data Series"
    >
      Sort the series by either ascending or descending order when possible to
      aid readability. Order by date to show trends over time.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image4.src}
      imgAlt="Image showing a bar chart where values on the y-axis start at zero."
      captionTitle="Start At Zero"
    >
      Ensure the numerical axes start at zero so the bars reflect accurate
      proportions compared to one another.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
