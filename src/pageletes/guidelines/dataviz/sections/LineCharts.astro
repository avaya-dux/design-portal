---
import {
  CollapsibleColumns,
  Description,
  ImageWithCaption,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/data-vis-line-charts-1.svg";
import image2 from "../imgs/data-vis-line-charts-2.svg";
import image3 from "../imgs/data-vis-line-charts-3.svg";
import image4 from "../imgs/data-vis-line-charts-4.svg";
---

<PageleteSection>
  <SectionHeader id={topics.linecharts.id}>
    {topics.linecharts.title}
  </SectionHeader>

  <Description>
    Line charts are used to show continuous data, show trends, or to compare
    data over time.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a standard linechart."
      captionTitle="Standard Line Chart"
    >
      Displays one or more lines to reflect changes over time.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing a sparkline."
      captionTitle="Sparkline"
    >
      Provides a compact representation of trends next to data values.
    </ImageWithCaption>
  </CollapsibleColumns>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image3.src}
      imgAlt="Image showing an solid line chart as actual data and dotted line as predicted."
      captionTitle="Actual Versus Predicted Data"
    >
      Use solid lines to display real data, and dashed lines for predicted data.
      Avoid smoothing the lines as it will obscure the data points.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image4.src}
      imgAlt="Image showing line chart with datapoints that don't line up in the increments in the x and y axis."
      captionTitle="Data Points On Uneven Increments"
    >
      Data points can be useful when displaying information that isn't collected
      in even increments. Avoid displaying these points when they become too
      dense.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
