---
import {
  CollapsibleColumns,
  Description,
  ImageWithCaption,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/data-vis-pie-charts-1.svg";
import image2 from "../imgs/data-vis-pie-charts-2.svg";
---

<PageleteSection>
  <SectionHeader id={topics.piecharts.id}>
    {topics.piecharts.title}
  </SectionHeader>

  <Description>
    Pie charts are used to show percentages and proportional data.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a pie chart with 5 slices."
      captionTitle="Use Minimal Categories"
    >
      Avoid using more that 5 categories in a pie chart as the data will become
      difficult to digest. If more categories are required, use a bar chart
      instead.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing a pie chart slices with a clear gap between them."
      captionTitle="Provide Whitespace Between Values"
    >
      Use whitespace between slices to provide clear contrast between elements.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
