---
import SectionLayout from "../../SectionLayout.astro";

import graphic from "../../../../imgs/user-interface-1.svg";

import { Description } from "components";

const altText = "Graphic representing various elements";
const type = "Native Elements";
---

<SectionLayout
  graphicIcon={graphic.src}
  graphicAltText={altText}
  type={type}
>
<Description slot="description">Applications built for Windows should use native components wherever possible to ensure consistency across platforms, while leveraging built-in accessibility. Neo fonts, colors, and icons should be applied at the design token level where possible.</Description>
</SectionLayout>
