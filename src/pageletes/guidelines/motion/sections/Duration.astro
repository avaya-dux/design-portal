---
import {
	CollapsibleColumns,
	Description,
	ImageWithCaption,
	PageleteSection,
	SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/motion-duration-1.svg";
import image2 from "../imgs/motion-duration-2.svg";
import image3 from "../imgs/motion-duration-3.svg";
---

<PageleteSection>
  <SectionHeader id={topics.duration.id}>{topics.duration.title}</SectionHeader>

  <Description>
    When an on-screen element changes from one thing to another, it may be
    configured to happen over time. When a field value changes, or when a mouse
    hovers over something, the duration of the animation specifies how long the
    change takes.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a speed gauge with its marker on slow."
      captionTitle="Slow"
    >
      Use this for transitions that take 0.9 seconds to occur.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing a speed gauge with its marker on medium."
      captionTitle="Medium"
    >
      These transitions are 0.4 seconds in length. This is the primary speed
      used throughout Neo. It is used for hover states, opening and closing,
      sheets sliding in and out of the page, etc.
    </ImageWithCaption>
  </CollapsibleColumns>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image3.src}
      imgAlt="Image showing a speed gauge with its marker on fast."
      captionTitle="Fast"
    >
      The quickest transitions are 0.15 seconds in length. This is occasionally
      used in Neo for expanding table rows.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
