---
import {
  CollapsibleColumns,
  Description,
  ImageWithCaption,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";

import image1 from "../imgs/motion-easing-1.svg";
import image2 from "../imgs/motion-easing-2.svg";
import image3 from "../imgs/motion-easing-3.svg";
---

<PageleteSection>
  <SectionHeader id={topics.easing.id}>{topics.easing.title}</SectionHeader>

  <Description>
    Easing refers to how a moving UI element changes speed over time. Neo has 3
    types of easing tokens available.
  </Description>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image1.src}
      imgAlt="Image showing a circle shape animating side to side."
      captionTitle="Ease"
    >
      Elements can slide in and out from the screen in any of the 4 directions.
      Motion is quickest at the edges and slows down toward the middle of the
      screen.
    </ImageWithCaption>

    <ImageWithCaption
      imgSrc={image2.src}
      imgAlt="Image showing a circle shape easing in from the left."
      captionTitle="Ease-in"
    >
      When an element comes onto the screen, it can ease-in, either from the
      right or the left of the screen. It starts by moving quickly, but rapidly
      slows down as it reaches its final position.
    </ImageWithCaption>
  </CollapsibleColumns>

  <CollapsibleColumns>
    <ImageWithCaption
      imgSrc={image3.src}
      imgAlt="Image showing a circle shape easing out of the screen to the right."
      captionTitle="Ease-out"
    >
      When an element leaves the screen, it can ease-out, either to the right or
      the left of the screen. It starts by moving slowly, but rapidly speeds up
      as it disappears off the edge.
    </ImageWithCaption>
  </CollapsibleColumns>
</PageleteSection>
