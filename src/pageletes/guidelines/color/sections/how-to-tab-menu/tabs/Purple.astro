---
import { PageleteSubSection } from "components";

import ThreeColumnLayout from "../ThreeColumnLayout.astro";
import InfoBlockLayout from "../InfoBlockLayout.astro";

import observingImage from "../../../imgs/how-to-images/purple-supervisor-observing.svg";

const observingBlock = {
  image: observingImage.src,
  altText: "graphic representing greens use for an observing agent",
  header: "Supervisor and Observing Agents",
  description: "Related to messaging and states for these users.",
};
---

<PageleteSubSection marginTop="0" marginBottom="0">
  <ThreeColumnLayout>
    <InfoBlockLayout
      slot="left-frame"
      graphicIcon={observingBlock.image}
      graphicAltText={observingBlock.altText}
      header={observingBlock.header}
      description={observingBlock.description}
    />
  </ThreeColumnLayout>
</PageleteSubSection>
