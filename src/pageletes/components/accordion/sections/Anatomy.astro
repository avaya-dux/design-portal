---
import { Image } from "@avaya/neo-react";
import {
  ColumnLayout,
  Description,
  InfoTable,
  InfoTableBody,
  InfoTableCell,
  InfoTableRow,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";

import AnatomyImage from "../imgs/anatomy.svg";
const anatomyImgAltText =
  "Image showing the different parts that make up an accordion";
---

<PageleteSection>
  <SectionHeader id={topics.anatomy.id}>{topics.anatomy.title}</SectionHeader>

  <Description>
    Accordions include standard components as shown below, all of which are
    required. The number of items within the accordion list can vary.
  </Description>

  <ColumnLayout numOfColumns={1}>
    <Image src={AnatomyImage} alt={anatomyImgAltText} />
  </ColumnLayout>

  <InfoTable>
    <InfoTableBody>
      <InfoTableRow>
        <InfoTableCell>
          <strong>1. Collapsed accordion:</strong> The default state of the accordion.
          It occupies one line on the page.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>2. Header:</strong> This text field explains the content within
          the accordion. It should be a single line of text that clearly explains
          what is inside.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>3. Chevron Icon:</strong> The icon toggles the state of the accordion
          between Expanded and Collapsed. The icon changes to reflect the current
          state.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>4. Expanded accordion:</strong> When the chevron is activated,
          the accordion expands to reveal its content.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>5. Panel:</strong> This is the content within the accordion: instructions,
          links, descriptions, etc.
        </InfoTableCell>
      </InfoTableRow>
    </InfoTableBody>
  </InfoTable>
</PageleteSection>
