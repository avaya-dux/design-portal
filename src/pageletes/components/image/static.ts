import type { TopicsDict } from "components";

type ImageTopicNames = "playground";

export const topics: TopicsDict<ImageTopicNames> = {
  playground: {
    order: 0,
    id: "image-playground",
    title: "Interactive Playground",
  },
};
