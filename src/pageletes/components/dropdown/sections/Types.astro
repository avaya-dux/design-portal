---
import { Image } from "@avaya/neo-react";

import {
  ColumnLayout,
  Description,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
  StackedLayout,
} from "components/astro";

import { topics } from "../static";

import {
  TypesCheckboxImage,
  TypesDefaultImage,
  TypesGroupImage,
  TypesSelectedImage,
} from "../imgs";
import { DoAndDont } from "components";

const DoAltText = "Use dividers to group related items.";

const DontAltText = "Do not place dividers between each item.";
---

<PageleteSection>
  <SectionHeader id={topics.types.id}>{topics.types.title}</SectionHeader>

  <Description>
    Dropdowns can be deployed using different types depending upon the options
    and formatting requirements of the action items.
  </Description>

  <ColumnLayout showGridlines={true} bkgColor="white" numOfColumns={2}>
    <StackedLayout label="Default">
      <Image
        src={TypesDefaultImage}
        alt="Image showing default dropdown state"
      />
    </StackedLayout>
    <StackedLayout label="Selected">
      <Image
        src={TypesSelectedImage}
        alt="Image showing selected dropdown state"
      />
    </StackedLayout>
    <StackedLayout label="Checkbox">
      <Image
        src={TypesCheckboxImage}
        alt="Image showing dropdown with checkboxes"
      />
    </StackedLayout>
    <StackedLayout label="Grouped">
      <Image
        src={TypesGroupImage}
        alt="Image showing dropdown with grouped sections"
      />
    </StackedLayout>
  </ColumnLayout>

  <InfoList type="decimal">
    <InfoListItem label="Default">
      The default type shows the action item list. Selecting any option
      activates it and closes the dropdown.
    </InfoListItem>

    <InfoListItem label="Selected">
      When an action items is selected, it is tagged with a checkmark and the
      dropdown remains open. If the dropdown is opened again, the currently
      selected item will still be tagged.
    </InfoListItem>

    <InfoListItem label="Checkbox">
      In situations where more than one action item can be selected, use the
      checkbox type.
    </InfoListItem>

    <InfoListItem label="Grouped with Titles">
      This type is used to organize the action items into easily understandable
      sections. This makes it easier to find items in longer lists.
    </InfoListItem>
  </InfoList>
</PageleteSection>
