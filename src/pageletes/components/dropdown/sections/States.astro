---
import { Image } from "@avaya/neo-react";
import {
  Description,
  ColumnLayout,
  InfoTable,
  InfoTableBody,
  InfoTableCell,
  InfoTableRow,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";

import { StatesImage } from "../imgs";
const statesImgAltText =
  "Image showing the different parts that make up a dropdown";
---

<PageleteSection>
  <SectionHeader id={topics.states.id}>{topics.states.title}</SectionHeader>

  <Description>
    An dropdown exists in one of 4 states. The state changes as the user
    interacts with the action items.
  </Description>

  <ColumnLayout bkgColor="white" numOfColumns={1}>
    <Image src={StatesImage} alt={statesImgAltText} />
  </ColumnLayout>

  <InfoTable>
    <InfoTableBody>
      <InfoTableRow>
        <InfoTableCell>
          <strong>1. Default:</strong> When the dropdown is opened, the action items
          are shown.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>2. Hover:</strong> As the focus is moved over the mneu items, they
          are highlighted and can then be selected.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>3. Disabled:</strong> Some circumstances will prevent one or more
          items from being selectable.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>4. Selected:</strong> When an item is selected, it is shown with
          a checkmark beside the item. Some lists allow more than one selection to
          be made.
        </InfoTableCell>
      </InfoTableRow>
    </InfoTableBody>
  </InfoTable>
</PageleteSection>
