---
import { Image } from "@avaya/neo-react";

import {
	ColumnLayout,
	Description,
	DoAndDont,
	ImageWrapper,
	PageleteSection,
	PageleteSubSection,
	SectionHeader,
	StackedLayout,
	SubHeader,
} from "components";

import { topics } from "../static";

const { id, title } = topics.behavior;

import placementImage1 from "../imgs/pagination-behavior-placement-1.svg";
const placementImgAltText1 =
	"Shows appropriate positioning for the pagination bar.";

import placementImage2 from "../imgs/pagination-behavior-placement-2.svg";
const placementImgAltText2 =
	"Shows appropriate positioning for the pagination bar with pages with fewer rows.";

import placementImage3 from "../imgs/pagination-behavior-items-1.svg";
const placementImgAltText3 =
	"Shows how items per a page can effect the pagination bars position on a device.";

import variationImage1 from "../imgs/pagination-behavior-variations-1.svg";
const variationImgAltText1 = "Shows image of the full version of the pagination bar.";

import variationImage2 from "../imgs/pagination-behavior-variations-2.svg";
const variationImgAltText2 = "Shows image of the condensed version of the pagination bar.";

import doImg1 from "../imgs/pagination-behavior-placement-do-1.svg";
import dontImg1 from "../imgs/pagination-behavior-placement-dont-1.svg";
const doInstructions1 =
	"The pagination remains fixed at the bottom of the page, ensuring it is always visible.";
const doImgAltText1 =
	"image showing the pagination component fixed at bottom of the page.";
const dontInstructions1 = "Do not place the pagination above the table.";
const dontImgAltText1 =
	"image showing the pagination component above the table.";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    The implementation of pagination varies depending upon the size of the
    table and the size of the page showing the data.
  </Description>

  <PageleteSubSection>
    <SubHeader id="pagination-placement">Placement</SubHeader>

    <Description>
      The pagination bar is always visible and positioned at the bottom of the viewable page.
    </Description>

    <ColumnLayout marginBottom={"3rem"} showGridlines={true} numOfColumns={1}>
      <StackedLayout isShaded vPadding="0px">
        <Image src={placementImage1.src} alt={placementImgAltText1} />
      </StackedLayout>
    </ColumnLayout>

    <Description>
      On pages with fewer rows, the pagination stays visible at the bottom of the page,
			not directly beneath the last row.
    </Description>

    <ColumnLayout marginBottom={"3rem"} showGridlines={true} numOfColumns={1}>
      <StackedLayout isShaded vPadding="0px">
        <Image src={placementImage2.src} alt={placementImgAltText2} />
      </StackedLayout>
    </ColumnLayout>

		<DoAndDont size="wide">
			<DoAndDont.DoContainer instructions={doInstructions1}>
				<DoAndDont.ImageExampleContainer>
					<Image src={doImg1.src} alt={doImgAltText1} />
				</DoAndDont.ImageExampleContainer>
			</DoAndDont.DoContainer>

			<DoAndDont.DontContainer instructions={dontInstructions1}>
				<DoAndDont.ImageExampleContainer>
					<Image src={dontImg1.src} alt={dontImgAltText1} />
				</DoAndDont.ImageExampleContainer>
			</DoAndDont.DontContainer>
		</DoAndDont>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="pagination-items">Items Per Page</SubHeader>

    <Description>
      The number of rows to show in a table depends on how much data is being
      presented, the screen size and resolution of the device being used, and
      user preferences.<br /><br />The user can choose to display 20, 50, or 100
      rows or data per page. The default value is 20 rows per page.
    </Description>

    <ColumnLayout marginBottom={"3rem"} showGridlines={true} numOfColumns={1}>
      <StackedLayout isShaded vPadding="0px">
        <Image src={placementImage3.src} alt={placementImgAltText3} />
      </StackedLayout>
    </ColumnLayout>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="pagination-variations">Variations</SubHeader>

    <Description>
      Pagination comes in two forms: full and condensed. Use the full version when there is
			ample space to display all details. Select the condensed version when space is limited.
			If there are no additional pages, a pagination bar is unnecessary.
    </Description>

    <ColumnLayout marginBottom={"3rem"} showGridlines={false} numOfColumns={1}>
      <StackedLayout showBorders labelPaddingTop="2rem" labelPaddingLeft="1rem" label="Full version" isShaded vPadding="0px" gap="2rem">
				<ImageWrapper isShaded showBorders={false} marginTop="0" padding="0rem 2.5rem 1.5rem">
        	<Image src={variationImage1.src} alt={variationImgAltText1} />
				</ImageWrapper>
      </StackedLayout>
      <StackedLayout showBorders labelPaddingTop="2rem" labelPaddingLeft="1rem" label="Condensed version" isShaded vPadding="0px">
				<ImageWrapper isShaded showBorders={false} marginTop="0" padding="0rem 2.5rem 1.5rem">
        	<Image src={variationImage2.src} alt={variationImgAltText2} />
				</ImageWrapper>
      </StackedLayout>
    </ColumnLayout>
  </PageleteSubSection>
</PageleteSection>
