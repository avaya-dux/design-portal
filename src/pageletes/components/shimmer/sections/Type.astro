---
import { Image } from "@avaya/neo-react";

import {
  Description,
  PageleteSection,
  SectionHeader,
  PageleteSubSection,
  SubHeader,
  ColumnLayout,
} from "components/astro";

import { DoAndDont } from "components/react";

import { topics } from "../static";

import DynamicImage from "../imgs/shimmer-content-do-1.svg";
import StaticImage from "../imgs/shimmer-content-dont-1.svg";
import progressiveLoadingImage from "../imgs/shimmer-progressive-loading.svg";

const { id, title } = topics.type;

const dynamicImgAltText = "Image showing dynamic content";
const staticImgAltText = "Image showing static content";
const dynamicContentInstruction =
  "Use the actual content for parts of the page that do not change (static content). For elements that are quick to load, such as headings, body copy, buttons, and borders, include these as static elements of the design. Shimmer load only the variable content, not the containers.";
const staticContentInstruction =
  "Do not shimmer load all of the elements in the window as this will extend the time needed for the page appear.";
const progressiveLoadingImgAltText = "Image showing progressive loading";
---

<PageleteSection>
  <SectionHeader id={id}>
    {title}
  </SectionHeader>
  <Description>
    There are two variants for a shimmer that affect the way pages are loaded
    and what bits appear first. The format and use of each is outlined below.
  </Description>
  <PageleteSubSection>
    <SubHeader id="shimmer-type-content">
      Dynamic Content vs Static Content
    </SubHeader>
    <Description>
      Certain elements on a page never change, such as headers, titles, copy
      text, etc. These elements will be loaded first as they can be processed
      quickly. Page content that changes, such as table data, graphs and
      settings, will be loaded after the static content is complete.
    </Description>
    <DoAndDont size="wide">
      <DoAndDont.DoContainer instructions={dynamicContentInstruction}>
        <DoAndDont.ImageExampleContainer>
          <Image src={DynamicImage} alt={dynamicImgAltText} />
        </DoAndDont.ImageExampleContainer>
      </DoAndDont.DoContainer>

      <DoAndDont.DontContainer instructions={staticContentInstruction}>
        <DoAndDont.ImageExampleContainer>
          <Image src={StaticImage} alt={staticImgAltText} />
        </DoAndDont.ImageExampleContainer>
      </DoAndDont.DontContainer>
    </DoAndDont>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="shimmer-type-loading">Progressive Loading</SubHeader>
    <Description>
      Progressive loading is a technique that loads pages in batches. The
      fastest items to load go first, followed by progressively more detailed
      items.
    </Description>
    <ColumnLayout bkgColor="white" numOfColumns={1} marginTop={"3rem"}>
      <Image src={progressiveLoadingImage} alt={progressiveLoadingImgAltText} />
    </ColumnLayout>
    <Description>
      The primary batch should show a page’s basic structure (the skeleton state
      versions of the container-based components), data-based text (the skeleton
      state version of text) and non-data text. Following batches can include
      images, content outside of the viewport, interactive (action-based)
      components, and variable text.
    </Description>
  </PageleteSubSection>
</PageleteSection>
