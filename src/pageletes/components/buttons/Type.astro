---
import { Image } from "@avaya/neo-react";

import {
  ColumnLayout,
  Description,
  ImageWrapper,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
} from "components/astro";
import image1 from "./button-imgs/button-type-1.svg";
import image2 from "./button-imgs/button-type-2.svg";
import image3 from "./button-imgs/button-type-3.svg";
import { topics } from "./static";

const image1Alt = "Image showing a primary button";
const image2Alt = "Image showing a secondary button";
const image3Alt = "Image showing a tertiary button";
---

<PageleteSection>
  <SectionHeader id={topics.type.id}>{topics.type.title}</SectionHeader>

  <Description>
    There are multiple variants for each button. The format and use of each is
    outlined below.
  </Description>

  <ColumnLayout numOfColumns={3}>
    <ImageWrapper showBorders={false} marginTop={"0rem"} marginBottom={"0rem"}>
      <Image src={image1} alt={image1Alt} />
    </ImageWrapper>
    <ImageWrapper showBorders={false} marginTop={"0rem"} marginBottom={"0rem"}>
      <Image src={image2} alt={image2Alt} />
    </ImageWrapper>
    <ImageWrapper showBorders={false} marginTop={"0rem"} marginBottom={"0rem"}>
      <Image src={image3} alt={image3Alt} />
    </ImageWrapper>
  </ColumnLayout>

  <InfoList type="disc">
    <InfoListItem label="Primary Button">
      Use these buttons to perform the most important action item on the current
      page. For example, Save or Continue. Too many Primary buttons can be
      confusing for the user, so only include one Primary button on each screen
      or region.
    </InfoListItem>

    <InfoListItem label="Secondary Button">
      Secondaries offer alternate or opposite actions to the Primary. For
      example, Cancel, Back, or Reset. Multiple Secondary buttons can be used
      with each Primary.
    </InfoListItem>

    <InfoListItem label="Tertiary Button">
      Use them on their own or pair them up with other buttons. Tertiary buttons
      contain the least important action items on the page.
    </InfoListItem>
  </InfoList>
</PageleteSection>
