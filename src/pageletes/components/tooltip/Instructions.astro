---
import {
  Description,
  PageleteSection,
  PageleteSubSection,
  SectionHeader,
  SubHeader,
} from "components/astro";

import { topics } from "./static";
---

<PageleteSection>
  <SectionHeader id={topics.basics.id}>{topics.basics.title}</SectionHeader>

  <Description>
    The following are the basic rules for implementing a tooltip via HTML+CSS
    with the Neo CSS library.
  </Description>

  <PageleteSubSection>
    <SubHeader id="tooltip-instructions-basics">Basics</SubHeader>

    <Description>
      To add a tooltip to an element, wrap them in a <code>{"div"}</code> with the
      <code>{"neo-tooltip"}</code> and
      <code>{"neo-tooltip--onhover"}</code> classes. You also need to specifiy the
      tooltip position by adding the
      <code>{"neo-tooltip--<modifier>"}</code>
      class with the appropriate direction.
    </Description>

    <Description>
      Then add a div element with the class
      <code>{"neo-tooltip__content"}</code>. This
      <code>{"div"}</code> should contain a nested <code>{"div"}</code> with class
      <code>{"neo-arrow"}</code>, and the text of the tooltip itself.
    </Description>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="tooltip-instructions-positioning">Positioning</SubHeader>

    <Description>
      You can use the following classes to position the tooltip:
      <code>{"neo-tooltip--up-right"}</code>,
      <code>{"neo-tooltip--up-left"}</code>,
      <code>{"neo-tooltip--down-right"}</code>,
      <code>{"neo-tooltip--down-left"}</code>.
    </Description>

    <Description>
      Note that these should only be used where tight spacing precludes the use
      of the default Tooltip alignment.
    </Description>
  </PageleteSubSection>
</PageleteSection>
