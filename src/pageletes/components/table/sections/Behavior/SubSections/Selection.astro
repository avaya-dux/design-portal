---
import { Image } from "@avaya/neo-react";

import { Description, ImageWrapper, SubHeader } from "components";

import img1 from "./imgs/table-behavior-selection-1.svg";
const img1Alt = "Image showing a selectable table with three rows selected";

import img2 from "./imgs/table-behavior-selection-2.svg";
const img2Alt =
	"Image showing a selectable table, the selection dropdown is opened and the user is able to select all the items in the table, all the shown items, or clear the selection";

import img3 from "./imgs/table-behavior-selection-3.svg";
const img3Alt =
	"Image showing a selectable table with all items shown selected, and a clickable 'select all items' message shown";

import img4 from "./imgs/table-behavior-selection-4.svg";
const img4Alt =
	"Image showing a selectable table with all items selected, and a 'clear all selections' button shown";
---

<SubHeader id="table-behavior-selection">Selection</SubHeader>

<Description>
  One or more rows of the table can be selected. Any action taken (i.e. delete)
  will be applied to all of the selected records. Clicking the checkbox selects
  that record.
</Description>

<ImageWrapper isShaded marginBottom={"3rem"}>
  <Image src={img1.src} alt={img1Alt} />
</ImageWrapper>

<Description>
  Use the checkbox in the header to select all the items on the current page.
  Use the dropdown menu to select all items in the database.
</Description>

<ImageWrapper isShaded marginBottom={"3rem"}>
  <Image src={img2.src} alt={img2Alt} />
</ImageWrapper>

<Description>
  If all the items on the page are selected, an inline message appears with the
  option to expand the selection to include all items in the table.
</Description>

<ImageWrapper isShaded marginBottom={"3rem"}>
  <Image src={img3.src} alt={img3Alt} />
</ImageWrapper>

<Description>
  The user can deselect all items in the table without having to visit every
  page by using the “Clear all selections” option. This also appears in the
  header dropdown menu. Unchecking the box in the header will unselect all items
  on the current page only.
</Description>

<ImageWrapper isShaded>
  <Image src={img4.src} alt={img4Alt} />
</ImageWrapper>
