---
import { Image } from "@avaya/neo-react";

import {
  ColumnLayout,
  Description,
  InfoList,
  InfoListItem,
  PageleteSection,
  PageleteSubSection,
  SectionHeader,
  StackedLayout,
  SubHeader,
} from "components/astro";

import { DoAndDont } from "components/react";

import { topics } from "../static";

import StandaloneIconDefault from "../imgs/link-behavior-standalone-icon-1.svg";
import StandaloneIconHover from "../imgs/link-behavior-standalone-icon-2.svg";
import StandaloneIconDisabled from "../imgs/link-behavior-standalone-icon-3.svg";

import InlineUsageImageDo from "../imgs/link-behavior-inline-do-1.svg";
import InlineUsageImageDont from "../imgs/link-behavior-inline-dont-1.svg";

const inlineUsageImgDoAltText = "Use blue underlined text for inline links to help it stand out.";

const inlineUsageImgDontAltText =
  "Do not include inline links that are not underlined regardless of their color as it can get lost.";

const { id, title } = topics.behavior;

import StandaloneUsageImageDo from "../imgs/link-behavior-standalone-do-1.svg";
import StandaloneUsageImageDont from "../imgs/link-behavior-standalone-dont-1.svg";

const standaloneUsageImgDoAltText =
  "Use standalone links when building a list or when used on their own. They are not underlined.";
const standaloneUsageImgDontAltText =
  "Do not underline standalone links in their default state.";
---

<PageleteSection>
  <SectionHeader id={id}>
    {title}
  </SectionHeader>
  <Description>
    Links are used to allow rapid navigation to other places within the same
    application, to other applications, to sites on the internet, or to access
    resources such as manuals or downloadable files.
  </Description>

  <PageleteSubSection>
    <SubHeader id="behavior-standalone-link">Standalone Link</SubHeader>
    <Description>
      Use a Standalone link whenever you need to provide navigation options that
      are not embedded into other text. The link text must be sufficiently
      descriptive of the link’s function. Do not include only the URL as the
      link text.
    </Description>

    <DoAndDont>
      <DoAndDont.DoContainer instructions={standaloneUsageImgDoAltText}>
        <DoAndDont.ImageExampleContainer>
          <Image
            src={StandaloneUsageImageDo}
            alt={standaloneUsageImgDoAltText}
          />
        </DoAndDont.ImageExampleContainer>
      </DoAndDont.DoContainer>

      <DoAndDont.DontContainer instructions={standaloneUsageImgDontAltText}>
        <DoAndDont.ImageExampleContainer>
          <Image
            src={StandaloneUsageImageDont}
            alt={standaloneUsageImgDontAltText}
          />
        </DoAndDont.ImageExampleContainer>
      </DoAndDont.DontContainer>
    </DoAndDont>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="behavior-standalone-link-with-icon">
      Standalone Link with Icon
    </SubHeader>
    <Description>
      Standalone links can include an icon either in front of or behind the
      text. The icon is not underlined. Only use icons for links for functions
      (i.e. downloading files, accessing data entry fields), never for
      navigation. The icon must be representative of the action, never for
      decoration.
    </Description>
    <ColumnLayout bkgColor="white" numOfColumns={3}>
      <StackedLayout label="Default" vPadding="2.8rem" gap="2.8rem">
        <Image
          src={StandaloneIconDefault}
          alt="Standalone Default Link with Icon"
        />
      </StackedLayout>

      <StackedLayout label="Hover" vPadding="2.8rem" gap="2.8rem">
        <Image
          src={StandaloneIconHover}
          alt="Standalone Hover Link with Icon"
        />
      </StackedLayout>

      <StackedLayout label="Disabled" vPadding="2.8rem" gap="2.8rem">
        <Image
          src={StandaloneIconDisabled}
          alt="Standalone Disabled Link Icon"
        />
      </StackedLayout>
    </ColumnLayout>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="behavior-inline-link">Inline Link</SubHeader>
    <Description>
      Use an Inline link to offer navigation options to the user from within
      other text. Inline links are part of a sentence, rather than being
      independent. They are colored blue and underlined to help them stand out
      against the other text.
    </Description>
    <Description>
      The link text must be descriptive of the link’s function, either the
      location where they will be taken or the action connected to the link.
    </Description>

    <DoAndDont>
      <DoAndDont.DoContainer instructions={inlineUsageImgDoAltText}>
        <DoAndDont.ImageExampleContainer>
          <Image src={InlineUsageImageDo} alt={inlineUsageImgDoAltText} />
        </DoAndDont.ImageExampleContainer>
      </DoAndDont.DoContainer>

      <DoAndDont.DontContainer instructions={inlineUsageImgDontAltText}>
        <DoAndDont.ImageExampleContainer>
          <Image src={InlineUsageImageDont} alt={inlineUsageImgDontAltText} />
        </DoAndDont.ImageExampleContainer>
      </DoAndDont.DontContainer>
    </DoAndDont>
  </PageleteSubSection>
</PageleteSection>
