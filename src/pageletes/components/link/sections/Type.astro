---
import { Image } from "@avaya/neo-react";
import {
  ColumnLayout,
  Description,
  InfoList,
  InfoListItem,
  PageleteSection,
  PageleteSubSection,
  SectionHeader,
  StackedLayout,
  SubHeader,
} from "components/astro";

import { topics } from "../static";

import StandaloneStateDefault from "../imgs/link-type-standalone-1.svg";
import StandaloneStateHover from "../imgs/link-type-standalone-2.svg";
import StandaloneStateDisabled from "../imgs/link-type-standalone-3.svg";

import InlineLinkStateDefault from "../imgs/link-type-inline-1.svg";
import InlineLinkStateHover from "../imgs/link-type-inline-2.svg";
import InlineLinkStateDisabled from "../imgs/link-type-inline-3.svg";

const { id, title } = topics.type;
---

<PageleteSection>
  <SectionHeader id={id}>
    {title}
  </SectionHeader>
  <Description>
    There are two link types that can be used in your applications.
  </Description>
  <PageleteSubSection>
    <SubHeader id="type-standalone">Standalone</SubHeader>
    <Description>
      Standalone Links are not embedded into the page’s text (e.g. in a sentence
      or a paragraph). They are used on their own, such as in a table of
      contents, a list, or a directory.
    </Description>
    <Description>
      Standalone links are never underlined since that would make them difficult
      to read when grouped with other standalone links. Icons may be attached to
      Standalone links if it would help the user understand the link’s function.
    </Description>

    <ColumnLayout bkgColor="white" numOfColumns={3}>
      <StackedLayout label="Default" vPadding="2.8rem" gap="2.8rem">
        <Image src={StandaloneStateDefault.src} alt="Standalone Default Link" />
      </StackedLayout>

      <StackedLayout label="Hover" vPadding="2.8rem" gap="2.8rem">
        <Image src={StandaloneStateHover.src} alt="Standalone Hover Link" />
      </StackedLayout>

      <StackedLayout label="Disabled" vPadding="2.8rem" gap="2.8rem">
        <Image
          src={StandaloneStateDisabled.src}
          alt="Standalone Disabled Link"
        />
      </StackedLayout>
    </ColumnLayout>

    <CaptionInfoList type="disc">
      <InfoListItem label="Default">
        The default state of the Standalone link is blue text without
        underlining. Use this type when the link does not have focus.
      </InfoListItem>

      <InfoListItem label="Hover">
        When the focus is on the link, it becomes underlined. Selecting the link
        will take the user to the specified location.
      </InfoListItem>

      <InfoListItem label="Disabled">
        A link that is disabled appears on the page but is inactive. This may
        occur when the user is required to take some other action first (i.e.
        create a user name, fill in a field, etc.) before the link becomes
        active.
      </InfoListItem>
    </InfoList>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="type-inline">Inline</SubHeader>
    <Description>
      An Inline Link is embedded into the text on the page. It is an active word
      or words that are part of a sentence. Inline links are underlined as this
      helps to make them stand out against the other text.
    </Description>

    <ColumnLayout bkgColor="white" numOfColumns={3}>
      <StackedLayout label="Default" vPadding="2.8rem" gap="2.8rem">
        <Image src={InlineLinkStateDefault.src} alt="Inline Default Link" />
      </StackedLayout>

      <StackedLayout label="Hover" vPadding="2.8rem" gap="2.8rem">
        <Image src={InlineLinkStateHover.src} alt="Inline Hover Link" />
      </StackedLayout>

      <StackedLayout label="Disabled" vPadding="2.8rem" gap="2.8rem">
        <Image src={InlineLinkStateDisabled.src} alt="Inline Disabled Link" />
      </StackedLayout>
    </ColumnLayout>

    <CaptionInfoList type="disc">
      <InfoListItem label="Default">
        The default state of the Inline link is underlined blue text. Use this
        type when the link does not have focus.
      </InfoListItem>

      <InfoListItem label="Hover">
        When the focus is on the link, the underlining is removed. Selecting the
        link will take the user to the specified location.
      </InfoListItem>

      <InfoListItem label="Disabled">
        A link that is disabled appears on the page but is inactive. This may
        occur when the user is required to take some other action first (i.e.
        create a user name, fill in a field) before the link becomes active.
      </InfoListItem>
    </InfoList>
  </PageleteSubSection>
</PageleteSection>
