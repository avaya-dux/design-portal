---
import { Image } from "@avaya/neo-react";
import {
  Description,
  ColumnLayout,
  PageleteSection,
  SectionHeader,
  SubHeader,
  InfoTable,
  InfoTableBody,
  InfoTableCell,
  InfoTableRow,
  StackedLayout,
} from "components/astro";

import { DoAndDont } from "components/react";

import { topics } from "../static";

import InlineLinkStateDefault from "../imgs/link-inline-1.svg";
import InlineLinkStateHover from "../imgs/link-inline-2.svg";
import InlineLinkStateDisabled from "../imgs/link-inline-3.svg";

import UsageImageDo from "../imgs/link-inline-do-1.svg";
import UsageImageDont from "../imgs/link-inline-dont-1.svg";

const usageImgDoAltText = "Use blue underlined text for inline links.";

const usageImgDontAltText =
  "Do not include inline links that are not underlined. ";
---

<PageleteSection>
  <SectionHeader id={topics.inline.id}>
    {topics.inline.title}
  </SectionHeader>

  <Description>
    An Inline Link appears as part of the text. It is an active word or words
    that are part of a sentence. Inline links are underlined as it helps to make
    them easily visible among the other text.
  </Description>

  <ColumnLayout bkgColor="white" numOfColumns={3}>
    <StackedLayout showBorder>
      <div>Default</div>
      <Image src={InlineLinkStateDefault} alt="Inline Default Link" />
    </StackedLayout>

    <StackedLayout showBorder>
      <div>Hover</div>
      <Image src={InlineLinkStateHover} alt="Inline Hover Link" />
    </StackedLayout>

    <StackedLayout showBorder>
      <div>Disabled</div>
      <Image src={InlineLinkStateDisabled} alt="Inline Disabled Link" />
    </StackedLayout>
  </ColumnLayout>
  <InfoTable isBulletedList>
    <InfoTableBody>
      <InfoTableRow>
        <InfoTableCell>
          <strong>1. Default:</strong>
          The default state of an Inline link is underlined blue text.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>2. Hover:</strong>
          When the user move focus onto the link, the underlining is removed. Selecting
          the link in this state will jump the user to the specified location.
        </InfoTableCell>
      </InfoTableRow>

      <InfoTableRow>
        <InfoTableCell>
          <strong>3. Disabled:</strong>
          A link that is disabled appears on the page, but is currently unavailable.
          For example, when something has to happen (i.e. create a user name, fill
          in a field) before the link becomes active.
        </InfoTableCell>
      </InfoTableRow>
    </InfoTableBody>
  </InfoTable>

  <SubHeader id="inline-link-usage">Usage</SubHeader>
  <Description>
    Use an Inline link to offer navigation options to the user from within other
    text. Inline links are part of a sentence, rather than being independent,
    and are colored blue and underlined to help them stand out against the other
    text.
  </Description>
  <Description>
    Make sure that the link text is descriptive of where the user will be taken
    once selected.
  </Description>
  <Description>
    For accessibility, aria-text must be included that explains where the link
    goes, and also if there are associated actions, such as to download a PDF
    file or go to a data entry screen.
  </Description>
  <DoAndDont>
    <DoAndDont.DoContainer instructions={usageImgDoAltText}>
      <DoAndDont.ImageExampleContainer>
        <Image src={UsageImageDo} alt={usageImgDoAltText} />
      </DoAndDont.ImageExampleContainer>
    </DoAndDont.DoContainer>

    <DoAndDont.DontContainer instructions={usageImgDontAltText}>
      <DoAndDont.ImageExampleContainer>
        <Image src={UsageImageDont} alt={usageImgDontAltText} />
      </DoAndDont.ImageExampleContainer>
    </DoAndDont.DontContainer>
  </DoAndDont>
</PageleteSection>
<style is:inline>
  div.stack-container > div:first-child {
    color: var(--neo-color-base-1000);
  }
</style>
