---
import { Description, PageleteSection, SectionHeader } from "components/astro";
import { PlaygroundImplementation } from "../playground";
import { topics } from "../static";
const { id, title } = topics.playground;
---

<PageleteSection>
  <SectionHeader id={id}>
    {title}
  </SectionHeader>

  <Description>
    The Interactive Playground allows you to experiment with various link styles
    and features. Once you get the results you want, copy the HTML or React code
    provided and paste it into your application.
  </Description>
  <Description>
    You can add the link text and destination on the code page and it will
    appear in the playground window.
  </Description>

  <PlaygroundImplementation client:load />
</PageleteSection>
