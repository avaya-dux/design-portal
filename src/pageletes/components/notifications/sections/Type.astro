---
import { Image } from "@avaya/neo-react";

import {
  Description,
  ImageWrapper,
  PageleteSection,
  PageleteSubSection,
  SectionHeader,
  SubHeader,
} from "components/astro";

import { topics } from "../static";

const { id, title } = topics.type;

import generalImage from "../imgs/notification-type-general-1.svg";
const generalImgAltText =
  "Image showing a general notification";

import inlineImage from "../imgs/notification-type-inline-1.svg";
const inlineImgAltText =
  "Image showing an inline notification";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    There are two variations of a notification: general and inline.
  </Description>

  <PageleteSubSection>
    <SubHeader id="notifications-general-notifications">
      General Notifications
    </SubHeader>

    <Description>
      Notifications appear at the bottom left corner of a dashboard. As this may
      appear over existing content, a faint drop shadow is used to separate
      itself from the background elements. If multiple notifications appear at
      the same time, they stack on top of each other.
    </Description>

    <ImageWrapper>
      <Image src={generalImage} alt={generalImgAltText} />
    </ImageWrapper>
  </PageleteSubSection>

  <PageleteSubSection>
    <SubHeader id="notifications-inline-notifications">
      Inline notifications
    </SubHeader>

    <Description>
      Inline notifications are embedded into the page such as in a form. As the
      inline notification is part of the form, it does not need the drop shadow
      effect.
    </Description>

    <ImageWrapper>
      <Image src={inlineImage} alt={inlineImgAltText} />
    </ImageWrapper>
  </PageleteSubSection>
</PageleteSection>
