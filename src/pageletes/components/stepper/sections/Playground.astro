---
import { Description, PageleteSection, SectionHeader } from "components/astro";
import { topics } from "../static";
import { PlaygroundImplementation } from "./PlaygroundImplementation";
const { id, title } = topics.playground;
---

<PageleteSection>
  <SectionHeader id={id}>
    {title}
  </SectionHeader>

  <Description>
    The Interactive Playground allows you to experiment with various stepper
    styles and features. Once you get the results you want, copy the HTML or
    React code provided and paste it into your application.
  </Description>

  <PlaygroundImplementation client:load />
</PageleteSection>
