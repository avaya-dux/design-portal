---
import { Description, SectionHeader } from "components/astro";
import { topics } from "../static";
---

<SectionHeader id={topics.icon.id}>{topics.icon.title}</SectionHeader>

<Description>
  To add an icon to a chip, add the icon class, example:
  <code>{"neo-icon-info"}</code>
</Description>
