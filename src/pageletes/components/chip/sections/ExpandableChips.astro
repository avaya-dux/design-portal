---
import { Description, SectionHeader } from "components/astro";
import { topics } from "../static";
---

<SectionHeader id={topics.expandable.id}>
  {topics.expandable.title}
</SectionHeader>

<Description>
  To add an expandable icon to a Chip, add the
  <code>{"neo-chip--expandable"}</code> class.
</Description>

<Description>
  These can be used with a Dropdown Component as in the following example.
</Description>
