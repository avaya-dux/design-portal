---
import { Image } from "@avaya/neo-react";
import {
  Description,
  ImageWrapper,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
} from "components/astro";
import { topics } from "../static";

const { id, title } = topics.anatomy;

import image1 from "../imgs/chip-anatomy-1.svg";
const img1AltText =
  "Image showing a chip, there are four notes pointed out and described below";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    Chips include the four components shown below. The icon and delete
    components are optional.
  </Description>

  <ImageWrapper>
    <Image src={image1.src} alt={img1AltText} />
  </ImageWrapper>

  <CaptionInfoList>
    <InfoListItem label="Container">
      The container is left-justified and expands to the right to hold the text
      in the chip.
    </InfoListItem>

    <InfoListItem label="Text">
      Add a description of the chip’s function here.
    </InfoListItem>

    <InfoListItem label="Delete Chip">
      For chips that support it, select this icon to close the chip. This will
      deselect the function or remove an item from a list.
    </InfoListItem>

    <InfoListItem label="Icon">
      When enabled, the icon provides a visual representation of the function of
      the chip; information, notification, alert, etc. This should not be used
      for decoration but must be an aid to understanding what the chip
      represents.
    </InfoListItem>
  </InfoList>
</PageleteSection>
