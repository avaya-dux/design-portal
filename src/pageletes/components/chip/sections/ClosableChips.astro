---
import { Description, PageleteSection, SectionHeader } from "components/astro";
import { topics } from "../static";
---

<PageleteSection>
  <SectionHeader id={topics.closable.id}>{topics.closable.title}</SectionHeader>

  <Description>
    To add a close icon to a Chip, add
    <code>{"neo-chip--close"}</code> class. If you are using plain HTML, you will
    also need to add the appropriate JavaScript to remove the Chip from the DOM.
  </Description>
</PageleteSection>
