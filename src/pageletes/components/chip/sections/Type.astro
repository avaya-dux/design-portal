---
import { Image } from "@avaya/neo-react";

import {
  ColumnLayout,
  Description,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
  StackedLayout,
} from "components/astro";
import { topics } from "../static";

const { id, title } = topics.type;

import image1 from "../imgs/chip-type-1.svg";
const img1AltText = "Image showing a default chip";

import image2 from "../imgs/chip-type-2.svg";
const img2AltText = "Image showing an info chip";

import image3 from "../imgs/chip-type-3.svg";
const img3AltText = "Image showing a success chip";

import image4 from "../imgs/chip-type-4.svg";
const img4AltText = "Image showing a warning chip";

import image5 from "../imgs/chip-type-5.svg";
const img5AltText = "Image showing an alert chip";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    There are multiple variants for each chip. The format and use of each is
    outlined below.
  </Description>

  <ColumnLayout bkgColor="white" numOfColumns={2} marginBottom={"0"}>
    <StackedLayout label="Default">
      <Image src={image1} alt={img1AltText} />
    </StackedLayout>

    <StackedLayout label="Info">
      <Image src={image2} alt={img2AltText} />
    </StackedLayout>
  </ColumnLayout>
  <ColumnLayout bkgColor="white" numOfColumns={3}>
    <StackedLayout label="Success">
      <Image src={image3} alt={img3AltText} />
    </StackedLayout>

    <StackedLayout label="Warning">
      <Image src={image4} alt={img4AltText} />
    </StackedLayout>

    <StackedLayout label="Alert">
      <Image src={image5} alt={img5AltText} />
    </StackedLayout>
  </ColumnLayout>

  <InfoList type="decimal">
    <InfoListItem label="Default">
      Whenever the contents of a chip do not conform to one of the other types
      (Info, Success, Warning, Alert), use the Default type.
    </InfoListItem>

    <InfoListItem label="Info">
      The Info type provides feedback on the current state of something, or
      about what is happening.
    </InfoListItem>

    <InfoListItem label="Success">
      This type is used to show that an operation was completed without any
      issues or errors.
    </InfoListItem>

    <InfoListItem label="Warning">
      Use this type when an operation has encountered a problem and the process
      may not have been completed successfully.
    </InfoListItem>

    <InfoListItem label="Alert">
      Similar to the Warning type, but the problem is more severe and likely
      caused the process to fail.
    </InfoListItem>
  </InfoList>
</PageleteSection>
