---
import { Description, SectionHeader } from "components/astro";
import { topics } from "../static";
---

<SectionHeader id={topics.container.id}>
  {topics.container.title}
</SectionHeader>

<Description>
  Chips can be styled by adding the <code>neo-chip</code> class and the appropriate
  modifier
  <code>{"neo-chip neo-chip--<modifier>"}</code>.
</Description>

<Description>
  A chips container has the <code>neo-chips</code> class. The children of a chips
  container should include the
  <code>neo-chips__item</code> class.
</Description>
