---
import { Description, PageTitle, PageleteSection, Topics } from "components";
import { topics } from "../static";
---

<PageleteSection>
	<PageTitle>Drawer</PageTitle>

	<Description>
		Drawers put additional content on a separate pane without changing the
		user's position in the UI. The drawer is placed on top of the existing page
		which is disabled until the drawer is closed.
	</Description>

	<Topics topics={topics} />
</PageleteSection>
