---
import {
  KeyboardInteractionsTable
} from ".";

import {
  PageleteSection,
  SectionHeader,
  Description,
} from "components/astro";
import { topics } from "../static";
---

<PageleteSection>
  <SectionHeader id={topics.interactions.id}>
    {topics.interactions.title}
  </SectionHeader>

  <Description>
    Accessibility requires that each item on the screen is reachable using the
    keyboard alone. The mouse is not always an option for some users.
  </Description>

  <Description>
    Keyboard navigation employs the following shortcuts.
  </Description>

  <KeyboardInteractionsTable />
</PageleteSection>
