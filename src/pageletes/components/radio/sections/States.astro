---
import { Image } from "@avaya/neo-react";

import {
  ColumnLayout,
  Description,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
  StackedLayout,
} from "components/astro";

import { topics } from "../static";

const { id, title } = topics.states;

import image1 from "./imgs/states-1.svg";
const img1AltText = "Image showing a radio button in the unselected state";

import image2 from "./imgs/states-2.svg";
const img2AltText = "Image showing a radio button in the selected state";

import image3 from "./imgs/states-3.svg";
const img3AltText = "Image showing a radio button in the disabled state";

import image4 from "./imgs/states-4.svg";
const img4AltText = "Image showing a radio button in the read only state";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    A radio button can exist in several states. When unselected, the associated
    function is turned off. The function is turned on when the button is
    selected. A disabled radio button’s state cannot be changed until some other
    action has occurred (e.g. configuring another field on the page). Read-only
    shows that the option has already been set and cannot be changed.
  </Description>

  <ColumnLayout bkgColor="white" numOfColumns={4}>
    <StackedLayout label="Unselected">
      <Image src={image1.src} alt={img1AltText} />
    </StackedLayout>

    <StackedLayout label="Selected">
      <Image src={image2.src} alt={img2AltText} />
    </StackedLayout>

    <StackedLayout label="Disabled">
      <Image src={image3.src} alt={img3AltText} />
    </StackedLayout>

    <StackedLayout label="Read-only">
      <Image src={image4.src} alt={img4AltText} />
    </StackedLayout>
  </ColumnLayout>

  <InfoList type="decimal">
    <InfoListItem label="Unselected">
      This shows that the option is not active.
    </InfoListItem>

    <InfoListItem label="Selected">
      This radio button’s controlled function is active.
    </InfoListItem>

    <InfoListItem label="Disabled">
      The function of this button is not available for selection. Some other
      action may be required to enable it.
    </InfoListItem>

    <InfoListItem label="Read-only">
      This option has been selected, usually on another screen, and cannot be
      changed from here. It is shown for information only.
    </InfoListItem>
  </InfoList>
</PageleteSection>
