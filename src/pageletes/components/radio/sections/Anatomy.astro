---
import {
  Description,
  ImageWrapper,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";
import { Image } from "@avaya/neo-react";

const { id, title } = topics.anatomy;

import image1 from "./imgs/anatomy.svg";
const img1AltText =
  "Image showing a radio group with one selection, there are four notes pointed out and described below";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    Radios are used to select a single option from a list of related options.
    They should include a label to identify the feature it controls if there is
    enough space on the screen.
  </Description>

  <ImageWrapper>
    <Image src={image1} alt={img1AltText} />
  </ImageWrapper>

  <InfoList type="decimal">
    <InfoListItem label="Radio Group Label (optional)">
      Identifies the common factor for this group of buttons.
    </InfoListItem>

    <InfoListItem label="Radio Label">
      This is the active element used to select the option. When selected, the
      radio button appears as a filled circle.
    </InfoListItem>

    <InfoListItem label="Radio Button">
      Shows the function controlled by that button.
    </InfoListItem>

    <InfoListItem label="Unselected Radio Button">
      The radio button is an empty circle while not selected.
    </InfoListItem>
  </InfoList>
</PageleteSection>
