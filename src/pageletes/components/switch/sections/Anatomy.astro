---
import {
  Description,
  ImageWrapper,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
} from "components/astro";

import { topics } from "../static";
import { Image } from "@avaya/neo-react";

const { id, title } = topics.anatomy;

import image1 from "../imgs/switch-anatomy.svg";
const img1AltText =
  "Image showing a toggled switch, there are three notes pointed out and described below";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    A switch can be built with or without labels. The label can be positioned to
    the left or right side of the switch.
  </Description>

  <ImageWrapper>
    <Image src={image1.src} alt={img1AltText} />
  </ImageWrapper>

  <InfoList type="decimal">
    <InfoListItem label="Track">
      This shows the current position of the handle (left or right).
    </InfoListItem>

    <InfoListItem label="Handle with Icon">
      The moveable piece of the switch. Move it to the right to enable the
      function, or to the left to disable it.
    </InfoListItem>

    <InfoListItem label="Label">
      The text description of the function that is controlled by the switch.
    </InfoListItem>
  </InfoList>
</PageleteSection>
