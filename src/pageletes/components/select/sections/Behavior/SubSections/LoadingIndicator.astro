---
import { Image } from "@avaya/neo-react";

import { Description, ImageWrapper, SubHeader } from "components";

import img1 from "./imgs/select-behavior-loading-1.svg";
const img1Alt =
	"Image showing a select in an loading state, which is expressed by there being a spinner in the select";
---

<SubHeader id="select-behavior-loading-indicators">Loading Indicator</SubHeader>

<Description>
  Whenever a select component needs time to collect data or transmit a
  selection, the field contains a Loading Indicator which includes a spinner.
  The selected data or the menu list appears once loading is finished.
</Description>

<ImageWrapper padding="0">
  <Image src={img1.src} alt={img1Alt} />
</ImageWrapper>
