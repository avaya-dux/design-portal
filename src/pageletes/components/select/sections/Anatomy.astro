---
import { Image } from "@avaya/neo-react";

import {
  ColumnLayout,
  Description,
  InfoList,
  InfoListItem,
  PageleteSection,
  SectionHeader,
  StackedLayout,
} from "components/astro";

import { topics } from "../static";

const { id, title } = topics.anatomy;

import image1 from "./imgs/select-anatomy-1.svg";
const img1AltText =
  "Image showing a select component with its four notes pointed out and described below";

import image2 from "./imgs/select-anatomy-2.svg";
const img2AltText =
  "Image showing an expanded select component with its expanded section noted as the fifth note and described below";
---

<PageleteSection>
  <SectionHeader id={id}>{title}</SectionHeader>

  <Description>
    The select component provides a list of options from which the user can
    select one or more items. The dropdown contains a list of pre-defined items
    that control some aspect of the application.
  </Description>

  <ColumnLayout bkgColor="white" numOfColumns={2}>
    <StackedLayout label="Select">
      <Image src={image1.src} alt={img1AltText} />
    </StackedLayout>

    <StackedLayout label="Select with Dropdown">
      <Image src={image2.src} alt={img2AltText} />
    </StackedLayout>
  </ColumnLayout>

  <InfoList type="decimal" fontSize="14px">
    <InfoListItem label="Label">
      A label should be included above the select field to describe its function
      to the user.
    </InfoListItem>

    <InfoListItem label="Form">
      This field displays the currently selected function (if any) for this
      component. This is chosen from the dropdown list. The user can start
      typing a response and matching items from the list will be displayed
      below.
    </InfoListItem>

    <InfoListItem label="Helper Text">
      This optional field provides additional information on the purpose of this
      field.
    </InfoListItem>

    <InfoListItem label="Chevron">
      The presence of the chevron icon shows that there is a menu of items that
      can be accessed by selecting the field.
    </InfoListItem>

    <InfoListItem label="Expand Trigger">
      Each of the selectable options are shown here. The user will choose one or
      more of the items to make active.
    </InfoListItem>
  </InfoList>
</PageleteSection>
